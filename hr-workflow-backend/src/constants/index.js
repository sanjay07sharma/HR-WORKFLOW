const { TASK_STATUS, TASK_STATUS_VALUES } = require('./taskStatus');
const { TASK_PRIORITY, TASK_PRIORITY_VALUES } = require('./taskPriority');
const { DEFAULT_CATEGORIES } = require('./categories');

module.exports = {
  TASK_STATUS,
  TASK_STATUS_VALUES,
  TASK_PRIORITY,
  TASK_PRIORITY_VALUES,
  DEFAULT_CATEGORIES
};
